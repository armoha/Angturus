from eudplib import *


class Monster:
    def __init__(self, name, unitID, STR, AGI, INT, CON, PER):
        # self.* : 인스턴스변수
        self.width = width
        self.height = height
        Rectangle.count += 1


mobDict = {
    "이름": ["STR", "AGI", "DEX", "INT", "CON", "WIT", "LV"],
    "고양이": EUDArray([5, 5, 5, 5, 5, 5, 1])
}


def f_getMobStats(name):
    return mobDict[name]
